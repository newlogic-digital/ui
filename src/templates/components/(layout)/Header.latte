<header class="x-header grid grid-cols-container" data-controller="x-header">
    <nav class="flex py-4 gap-4">
        <a n:foreach="$nav as $page" class="button muted" href="{$page->url}" title="{$page->name}">
            {$page->name}
        </a>
    </nav>
</header>
